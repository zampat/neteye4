{
  "id": "eventlog",
  "token": "atoken",
  "collector_config": {
    "event_type": "eventlog",
    "payload": {
      "source": "logstash",
      "host": "${host}",
      "message": "${message}",
      "winlog": "${winlog}"
    }
  }
}
