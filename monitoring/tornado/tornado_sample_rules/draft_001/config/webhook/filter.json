{
  "type": "filter",
  "name": "webhook",
  "description": "Webhook HTTP call collected by from \"tornado_webhook_collector\" service",
  "active": true,
  "filter": {
    "type": "AND",
    "operators": [
      {
        "type": "equals",
        "first": "${event.type}",
        "second": "hsg"
      }
    ]
  }
}